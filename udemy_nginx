docker container run --publish 80:80 nginx
docker container run --publish 80:80 --detach  nginx #runs container in the background
docker container ls -a
docker container logs webhost
docker container top webhost
docker container stop <e83>
docker container rm -f <e83 831 b32> #forces even the running containers to be deleted
docker container rm <41d>